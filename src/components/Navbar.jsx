import React from "react";

const Navbar = () => (
  <nav className="navbar navbar-dark bg-dark">
    <div className="container-fluid">
      <span className="navbar-brand mb-0 h1">
        <i className="fa fa-users me-2"></i>Admin Panel
      </span>
    </div>
  </nav>
);

export default Navbar;
